import PageHeader from "../Headings/PageHeader/PageHeader";

const ActivityFeedsPageHeader = () => {
    return (
        <PageHeader
            title="Activity Feeds"
            subtitle="View and track all route and driver activity history"
        />
    );
};

export default ActivityFeedsPageHeader;
